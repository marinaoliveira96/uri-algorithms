#include <stdio.h>
 
int main() {
 
    int A,B, soma;
 	
    scanf("%d", &A);
    scanf("%d", &B);
    soma = A + B;
    printf("Soma: %d + %d = %d", A, B, soma);
    //printf("X = %d\n", A+B);
 
    return 0;
}
